注意事项
1、AutoPackge.py、exportIPA.plist、Run.sh三个文件必须放在同一个文件夹内
2、直接双节run.sh即可运行，无需其他操作
3、需要用到python 第三方biplist
	sudo easy_install biplist
4、打包好的ipa全部存放在文件夹ipa内（ipa文件夹在运行之后会自动创建）
5、使用的是git下载或自动更新源代码，每次运行之前无需删除代码，
6、exportIPA.plist需要更改teamID以及打包模式 

打包参数—>需要修改AutoPackage.py文件内的各项参数
1、updateProjectFromGit()：函数内修改git地址
2、projectPath：工程目录
3、CODE_SIGN_IDENTITY：证书
4、PROVISIONING_PROFILE：配置文件
5、xcarchive_name：.xcarchive名称
6、ipa_name：.ipa名称
7、archive()：函数最后一个参数为打包的configuration
8、ftp相关
